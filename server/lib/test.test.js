const axios = require("axios");

describe("unit test for db queries", ()=>{
    describe("create listing", ()=>{
        const listing = {
            user_id,
            listing_name,
            description,
            timestamp,
            type,
            price,
            sold,
            condition,
            images,
            zipcode
        }
    })
})

